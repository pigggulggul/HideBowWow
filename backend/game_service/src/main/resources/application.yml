server:
  port: 8002
  domain: http://jongbum.site
  servlet:
    contextPath: "/api/game-service/"
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: game-service
  devtools:
    livereload:
      enabled: true

#logging
logging:
  level:
    org:
      springframework:
        messaging: DEBUG
        web:
          socket: DEBUG
      hibernate.orm.jdbc.bind: TRACE
    com.s10p31a709.game.common.aop: TRACE
