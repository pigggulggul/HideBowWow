server:
  port: 8002
  domain: http://jongbum.site
  servlet:
    contextPath: "/api/assign-service/"
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: assign-service
  devtools:
    livereload:
      enabled: true
  datasource:
    hikari:
      driver-class-name: org.mariadb.jdbc.Driver
      username: bum
      password: 12rlawhdqja!21
    url: jdbc:mariadb://database.cfsgaygiae4v.ap-northeast-2.rds.amazonaws.com:3306/s10p31a709

  # jpa
  jpa:
    hibernate:
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
      ddl-auto: update
    properties:
      hibernate:
        default_batch_fetch_size: 1000
        #        show-sql: true
        format_sql: true
#      open-in-view: false

#logging
logging:
  level:
    org:
      springframework.web: DEBUG
      hibernate.orm.jdbc.bind: TRACE
    com.s10p31a709.member.aop: TRACE
